
<aside class="left-off-canvas-menu">
  <ul class="off-canvas-list">
    <% if user_signed_in? %>
    <li><label><%=current_user.name%></label></li>
    <li><%= link_to image_tag("#{current_user.photo_location}"), edit_user_registration_path %></li>
    <% else %>
    <li><label>not signed in</label></li>
    <% end %>
    <li><a href="#">Some Link</a></li>
  </ul>
</aside>