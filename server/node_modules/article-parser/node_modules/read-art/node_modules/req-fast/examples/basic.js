var req = require('../');
req({
  uri: 'http://httpbin.org/ip',
  proxy: {
    host: '192.168.100.131',
    port: 3128
  }
}, function (err, resp) {
  if (err) {
    return console.log('[ERROR]', err.message);
  }

  console.log(resp);
});
